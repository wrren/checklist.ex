<h2>New list use</h2>

<%= render "form.html", changeset: @changeset,
                        action: list_use_path(@conn, :create, @list) %>

<%= link "Back", to: list_use_path(@conn, :index, @list) %>
